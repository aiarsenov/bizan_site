# Strapi Environment Variables (Локальная разработка)
# Скопируйте этот файл в .env

# Server
HOST=0.0.0.0
PORT=1337

# Database - PostgreSQL (из Docker)
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=bizan_strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapi_dev_password_123
DATABASE_SSL=false

# Secrets (сгенерируйте свои через: node -e "console.log(require('crypto').randomBytes(16).toString('base64'))")
APP_KEYS=toBeModified1,toBeModified2
API_TOKEN_SALT=toBeModified
ADMIN_JWT_SECRET=toBeModified
TRANSFER_TOKEN_SALT=toBeModified
JWT_SECRET=toBeModified

# Admin
ADMIN_PATH=/admin

# Redis (опционально, для будущего кэширования)
REDIS_HOST=localhost
REDIS_PORT=6379
