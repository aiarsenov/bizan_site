# ===========================================
# BIZAN SITE - Environment Variables
# ===========================================
# ВНИМАНИЕ: Скопируйте этот файл в .env и заполните реальными значениями
# Никогда не коммитьте .env в Git!

# -----------------------------------------
# ЛОКАЛЬНАЯ РАЗРАБОТКА (Development)
# -----------------------------------------
# Эти значения используются когда запускаете приложения нативно
# База данных берется из Docker (docker-compose.dev.yml)

# PostgreSQL (из Docker)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=bizan_strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapi_dev_password_123

# Redis (из Docker)
REDIS_HOST=localhost
REDIS_PORT=6379

# -----------------------------------------
# PRODUCTION (На сервере)
# -----------------------------------------
# Эти значения нужно будет заменить при деплое

# PostgreSQL
POSTGRES_DB=bizan_strapi
POSTGRES_USER=strapi
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# Redis
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD

# Strapi Secrets (генерируются при первом запуске)
# Сгенерировать можно через: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=
ADMIN_JWT_SECRET=
APP_KEYS=
API_TOKEN_SALT=

# Next.js
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337

# -----------------------------------------
# YANDEX CLOUD (для будущего этапа)
# -----------------------------------------
# YANDEX_STORAGE_BUCKET=
# YANDEX_STORAGE_ACCESS_KEY=
# YANDEX_STORAGE_SECRET_KEY=

# Email (Yandex SMTP)
# SMTP_HOST=smtp.yandex.ru
# SMTP_PORT=465
# SMTP_USERNAME=
# SMTP_PASSWORD=
