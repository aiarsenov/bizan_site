# 🏢 Сайт БИЗАН

Современный корпоративный сайт на Next.js 15 + Strapi 4.17 с Headless CMS архитектурой.

---

## 📋 Технологический стек

### Frontend
- **Next.js 15** (App Router, TypeScript)
- **React 19** + **TailwindCSS**
- **Framer Motion** (анимации)
- **React Hook Form** + **Zod** (формы и валидация)
- **Lucide React** (иконки)

### Backend (CMS)
- **Strapi 4.17** (Headless CMS, Node.js)
- **PostgreSQL 15** (основная БД)
- **Redis 7** (кэширование)

### Инфраструктура
- **Docker Compose** (контейнеризация)
- **Nginx** (reverse proxy, SSL)
- **Yandex Cloud** (хостинг)

---

## 🚀 Быстрый старт (для разработчиков)

### Требования
- **Node.js 20 LTS** (обязательно!)
- **Docker Desktop** ([скачать](https://www.docker.com/products/docker-desktop))
- **Git**

### ⚠️ Важно: Установка Node.js 20

**Используйте nvm для управления версиями Node.js:**

```bash
# Установить nvm (если еще нет)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Перезапустить терминал или:
source ~/.zshrc  # для macOS/Linux с zsh
source ~/.bashrc # для Linux с bash

# Установить Node.js 20 LTS
nvm install 20
nvm use 20
nvm alias default 20

# Проверить версию (должна быть v20.x.x)
node -v
```

**Почему именно Node.js 20?**
- Strapi 4.17 требует Node.js 18-20
- Node.js 24+ не поддерживается

---

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/aiarsenov/bizan_site.git
cd bizan_site
```

### 2️⃣ Установка зависимостей

```bash
# Установка зависимостей для всех проектов (root, frontend, backend)
npm run install:all
```

### 3️⃣ Настройка переменных окружения

```bash
# Корневой .env (опционально, пока не используется)
cp .env.example .env

# Backend (Strapi) - ОБЯЗАТЕЛЬНО
cd backend
cp .env.example .env
cd ..

# Frontend (Next.js) - ОБЯЗАТЕЛЬНО
cd frontend
cp .env.local.example .env.local
cd ..

# Для разработки можно оставить значения по умолчанию
```

**Важно:** Файлы `.env` и `.env.local` уже созданы с дефолтными значениями для разработки. Проверять не обязательно.

### 4️⃣ Запуск БД (PostgreSQL + Redis)

```bash
# Запустить БД в Docker (занимает ~30 секунд)
npm run db:start

# Проверить логи (опционально)
npm run db:logs
```

**Что произошло:**
- ✅ PostgreSQL доступен на `localhost:5432`
- ✅ Redis доступен на `localhost:6379`
- ✅ Данные сохраняются в Docker volumes (не потеряются при перезапуске)

### 5️⃣ Инициализация Strapi (только первый раз)

```bash
cd backend
npm run develop
```

- Откроется браузер на `http://localhost:1337/admin`
- Создайте первого администратора (запомните логин/пароль!)
- После создания можно закрыть (Ctrl+C)

### 6️⃣ Запуск приложений

**Вариант А: Запустить всё вместе (рекомендуется)**
```bash
# Из корня проекта
npm run dev
```

**Вариант Б: Запустить отдельно (2 терминала)**
```bash
# Терминал 1: Frontend
npm run dev:frontend

# Терминал 2: Backend (Strapi)
npm run dev:backend
```

### 7️⃣ Готово! 🎉

- **Frontend**: http://localhost:3000
- **Strapi Admin**: http://localhost:1337/admin
- **Strapi API**: http://localhost:1337/api

---

## 🛠️ Полезные команды

### База данных (Docker)
```bash
npm run db:start      # Запустить БД
npm run db:stop       # Остановить БД
npm run db:restart    # Перезапустить БД
npm run db:logs       # Посмотреть логи БД
```

### Разработка
```bash
npm run dev              # Запустить frontend + backend вместе
npm run dev:frontend     # Только frontend (Next.js)
npm run dev:backend      # Только backend (Strapi)
```

### Production (деплой на сервер)
```bash
npm run prod:build    # Собрать Docker образы
npm run prod:up       # Запустить весь стек в Docker
npm run prod:down     # Остановить
npm run prod:logs     # Логи production
```

### Очистка
```bash
npm run clean         # Удалить node_modules, кэши
```

---

## 📁 Структура проекта

```
bizan_site/
├── frontend/                    # Next.js приложение
│   ├── app/                     # App Router (Next.js 15)
│   ├── components/              # React компоненты
│   ├── public/                  # Статические файлы
│   └── package.json
│
├── backend/                     # Strapi CMS
│   ├── src/
│   │   ├── api/                 # API endpoints
│   │   └── admin/               # Admin customization
│   ├── config/                  # Конфигурация Strapi
│   └── package.json
│
├── docker/
│   ├── docker-compose.dev.yml   # Для разработки (только БД)
│   └── docker-compose.prod.yml  # Для production (весь стек)
│
├── .env.example                 # Пример переменных окружения
├── .gitignore
├── package.json                 # Root package.json со скриптами
└── README.md
```

---

## 🔐 Безопасность

### Важно для production:
1. **Смените все пароли** в `.env`:
   - `POSTGRES_PASSWORD`
   - `REDIS_PASSWORD`
   - `JWT_SECRET`, `ADMIN_JWT_SECRET`, `APP_KEYS`, `API_TOKEN_SALT`

2. **Сгенерировать секреты Strapi:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

3. **Никогда не коммитьте `.env`** в Git!

---

## 🐛 Troubleshooting

### Проблема: БД не запускается
```bash
# Проверьте, что Docker запущен
docker ps

# Посмотрите логи
npm run db:logs

# Перезапустите
npm run db:restart
```

### Проблема: Порты заняты
Если порты 3000, 1337, 5432 или 6379 заняты:
```bash
# Найдите процесс
lsof -i :3000
lsof -i :1337

# Остановите или измените порт в docker-compose.dev.yml
```

### Проблема: Strapi не подключается к БД
1. Убедитесь что БД запущена: `npm run db:start`
2. Проверьте `.env` - значения должны совпадать с `docker-compose.dev.yml`
3. Подождите 10-15 секунд после старта БД (инициализация)

---

## 📚 Документация

- [Next.js 15](https://nextjs.org/docs)
- [Strapi 4](https://docs.strapi.io/)
- [TailwindCSS](https://tailwindcss.com/docs)
- [Docker Compose](https://docs.docker.com/compose/)

---

## 👨‍💻 Разработка

### Workflow:
1. Создайте новую ветку: `git checkout -b feature/my-feature`
2. Сделайте изменения
3. Закоммитьте: `git commit -m "feat: описание изменений"`
4. Запушьте: `git push origin feature/my-feature`
5. Создайте Pull Request

---

## 📞 Контакты

- **Репозиторий**: https://github.com/aiarsenov/bizan_site
- **Email**: venom13@live.ru

---

**Приятной разработки! 🚀**
